#pragma once

#include <raylib.h>

const struct {
  const Color dark = GetColor(0x222222FF);
  const Color light = GetColor(0xFEFEFEFF);
  const Color primary = GetColor(0x19647EFF);
  const Color primary_lt = GetColor(0x28AFB0FF);
  const Color accent = GetColor(0xFF6B6BFF);
  const Color accent_lt = GetColor(0x92140CFF);
} Colors;
